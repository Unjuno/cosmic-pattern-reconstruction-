name: REAL DR11 point-random residual phase surrogate

on:
  workflow_dispatch:
  pull_request:
    branches: [experiment/dr11-point-random-selection-residual]
    paths:
      - 'src/point_random_phase_surrogate_validate.py'
      - '.github/workflows/real-dr11-point-random-phase-surrogate.yml'
      - 'docs/POINT_RANDOM_PHASE_SURROGATE.md'
      - 'run-trigger/point-random-phase-surrogate/**'

permissions:
  contents: read
  actions: read

jobs:
  point-random-phase-surrogate:
    runs-on: ubuntu-latest
    timeout-minutes: 70
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m py_compile src/point_random_selection_residual_validate.py src/point_random_phase_surrogate_validate.py
      - name: Materialize immutable expanded48 source artifact
        uses: actions/download-artifact@v4
        with:
          name: real-dr11-expanded48-32848895159
          path: .
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: 32848895159
      - name: Run point-random residual exact-power test
        run: |
          python src/point_random_phase_surrogate_validate.py \
            --fraction 0.04 \
            --surrogates 32 \
            --out results/real_dr11/point_random_phase_surrogate36
      - uses: actions/upload-artifact@v4
        with:
          name: real-dr11-point-random-phase-surrogate-${{ github.run_id }}
          path: results/real_dr11/point_random_phase_surrogate36
          if-no-files-found: error
          retention-days: 90
